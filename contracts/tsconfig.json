{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // This workspace runs via tsx/vitest and never emits; declaration emit would
    // trip TS2742/TS7056 on viem's deeply-generic client types.
    "declaration": false,
    "declarationMap": false
  },
  "include": ["src", "test", "scripts"]
}
